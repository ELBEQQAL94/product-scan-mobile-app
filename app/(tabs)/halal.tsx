import { FC } from "react";
import { View, Text } from "react-native";

const Halal: FC = () => {
  return (
    <View
      style={{
        flex: 1,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>halal</Text>
    </View>
  );
};

export default Halal;
