import {
  format_date_to_custom_string,
  format_date_to_timestamp,
} from "@/utils";

export const NOW_DATE = format_date_to_custom_string();
export const NOW_DATE_TIMESTAMP = format_date_to_timestamp();
